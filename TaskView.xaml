<UserControl x:Class="TestMakerWPF_FirstInstance.TaskView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TestMakerWPF_FirstInstance"
             mc:Ignorable="d" Height="80" Width="178.5">
    <Grid>
        <ContentControl x:Name="currentControl"></ContentControl>
        <Rectangle Fill="#FFF4F4F5" Stroke="Black" Height="80" VerticalAlignment="Bottom"/>
        <Label x:Name="TitleLabel" Content="Label" HorizontalAlignment="Center" Height="33" Margin="54,0,50,23" VerticalAlignment="Bottom" Width="158" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
        <Button x:Name="EditButton" Content="Edit" Height="33" Margin="0,24,10,0" VerticalAlignment="Top" HorizontalAlignment="Right" Width="40"/>
        <Button x:Name="DelButton" Content="Delete" HorizontalAlignment="Left" Height="33" Margin="10,24,0,0" VerticalAlignment="Top" Width="44"/>

    </Grid>
</UserControl>
